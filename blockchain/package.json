{
  "name": "civic-blockchain",
  "version": "1.0.0",
  "description": "Blockchain layer for civic issue logging",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "node scripts/deploy.js"
  },
  "dependencies": {
    "ethers": "^6.6.0",
    "hardhat": "^2.22.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0"
  }
}
